package de.htwg.poker.controller
//import de.htwg.poker.util.Command

/*class betCommand(amount: Int, controller: Controller) extends Command {
  def doStep: Unit = controller.gameState = controller.gameState.bet(
    controller.gameState.getSmallBlind,
    controller.gameState.getNextPlayer
  )
  def undoStep: Unit = controller.gameState = controller.gameState.bet(
    -controller.gameState.getSmallBlind,
    controller.gameState.getPreviousPlayer
  )
  def redoStep: Unit = controller.gameState = controller.gameState.bet(
    controller.gameState.getSmallBlind,
    controller.gameState.getPlayerAtTurn
  )
}*/
